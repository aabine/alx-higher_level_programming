#!/usr/bin/python3

def print_list_integer(my_list=[]):
    """
    Print the elements of a list on separate lines.

    Args:
        my_list (list): The list of elements to be printed.
    """
    for lists in range(len(my_list)):
        print("{}".format(my_list[lists]))
